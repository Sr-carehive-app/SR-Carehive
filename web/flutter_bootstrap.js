{{flutter_js}}
{{flutter_build_config}}

// Simple standard loader — renderer is set to "html" at BUILD TIME via
// "flutter build web --web-renderer html --release" so no override needed here.
_flutter.loader.load();


# Serechi
This repository contains a Flutter client (mobile/web) and a Node server used for transactional emails, payment notifications and small server-side helpers. A Supabase (Postgres) backend is used for authentication, database and storage.
